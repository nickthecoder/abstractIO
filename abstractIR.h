#ifndef abstractIR_h
#define abstractIR_h

/*
#include <IR.h>

class AbstractIR
{
  private :
    IRrecv *pReceiver;
    decode_results results;
    
  public :
    long value; // The value of the IR signal found during this loop, or 0 if no signal was seen.
  
  public :
    AbstractIR::AbstractIR( int pin );
    void loop(); // Call this from your sketch's loop function
};

class IRInput : public Input
{
  private :

  public:
    long button;

  public :
    IRInput( long button );
    
  public :
    virtual boolean keyPressed();
};
*/

#endif
