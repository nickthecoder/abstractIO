
To Do
=====

Create more example code
  Muxed analog inputs
    Using a single AnalogInput (all the same kind of inputs)
    Using different AnalogInputs (different kinds of inputs)
  Muxed analog inputs with a line select decoder chip (awaiting delivery!)

  Use a 164 and/or a 183 to select a chip.
    Note, I've written code for a 183, but can't tested it till the chips arrive from china.
    Could use the 595 shifts, and use a single pin for clock and latch (inverted?)

  Create boards to test each scenario
    Each board allows an Arduino Nano to be plugged onto it (or the pro mini?)
    Simple scenarios can go onto one board :
      Simple switches
      Fade LED
      Analog (pot, light sensor)
  595 shift register - add two shift registers, and 16 LEDs
  MuxAnalogInput
    16 LDRs in a 4x4 grid?
    Two 4051s, an inverter, a 138 and a 164
    Jumpers on the chip selects
      Either to the arduino&inverter, or the 138 or the 164.

Input
-----


AnalogInput
-----------


Output
------


PWMOutput
---------

Implement an external PWM output - waiting for one from China, especially for this!

IR Remote
---------

Other
-----

Rename "remote"
Create an IR Output (sends an IR signal) - One class per protocol.

RF Transmitter / Reciever
Create RFButton similar to the IRButton


